import { createContext, onCleanup, onMount } from "solid-js";
export const defaultPresenceContextState = {
    initial: () => true,
    addCleanup: onCleanup,
    addMount: onMount,
};
export const ParentContext = createContext();
export const PresenceContext = createContext(defaultPresenceContextState);
